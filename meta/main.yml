galaxy_info:
  author: Kevin Wittek
  description: Ansible role for using the docker_service module with docker-compose.yml files on the remote host.
  company: Styracosoft GbR

  license: Apache v2.0

  min_ansible_version: 2.2

  platforms:
  - name: Ubuntu
    versions:
      - xenial
      - yakkety

  galaxy_tags: 
    - docker
    - docker-compose
    - ci
    - cd
    - deployment
    - production

dependencies:
  - role: angstwad.docker_ubuntu